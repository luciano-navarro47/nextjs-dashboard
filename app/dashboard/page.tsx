export default function Page() {
    return <p>Esta pagina es el contenido del dashboard</p>
}

// Siempre hacer un export default de una funcion con cualquier nombre 
// para que funcione correctamente file system routing proporcionado por Nextjs

// Como se puede ver, la nueva ruta creada "automaticamente" por nextjs se llamará 
// igual que el nombre que le dimos a nuestra carpeta creada que contiene page.tsx 